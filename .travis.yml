language: csharp
sudo: required
dotnet: 2.2
before_install:
  - sudo apt-get install -y texlive texlive-lang-cyrillic
script:
  - dotnet build
after_success:
  - bash ./generate-pdf.sh
  - bash ./publish-docs.sh